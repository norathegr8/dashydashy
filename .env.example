# Only needed if running scripts manually (npm run pipeline)
# Not required when using the "analyze-creator" skill in Claude Code

APIFY_API_TOKEN=your_apify_token_here
TIKTOK_PROFILES=username_here
VIDEOS_PER_PROFILE=20
